{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "rules": {
      "type": "array",
      "description": "A list of conventional commit rules",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "description": "The type of the commit (e.g., feat, fix, docs)"
          },
          "emoji": {
            "type": "string",
            "description": "The emoji associated with the commit type"
          },
          "title": {
            "type": "string",
            "description": "The title of the commit type"
          },
          "description": {
            "type": "string",
            "description": "A detailed description of the commit type"
          }
        },
        "required": [
          "type",
          "emoji",
          "title",
          "description"
        ]
      }
    }
  },
  "required": [
    "rules"
  ]
}